package com.appvillis.nicegram_wallet.destinations

import android.content.Context
import android.content.Intent

object SendSelectICScreenDestination {
    @JvmStatic fun createIntent(context: Context): Intent = Intent()
    @JvmStatic fun launch(context: Context) {}
}
